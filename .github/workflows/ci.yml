on:
  push:
    branches:
    - '**'

jobs:
  test:
    runs-on: windows-11-arm

    steps:
    - name: test
      shell: bash
      run: |
        which pacman
        which curl
        which clang
        which gcc
        which go && go version
